# ── Python ────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.egg
*.egg-info/
dist/
build/
.eggs/
.venv/
venv/
env/
.env

# ── Jupyter ───────────────────────────────────────────────────────────────
.ipynb_checkpoints/
*.ipynb

# ── Análisis — resultados FINALES (versionados) ───────────────────────────
# Corrida final E0 vs EB (29 jun 2026): tablas y figuras del paper se versionan.
# Las figuras .png se embeben en docs/PAPER.md, por lo que deben estar en git.

# ── GIS pesado no usado por el modelo (>50 MB) ────────────────────────────
# El modelo solo carga red_vial_la_carolina.shp; estas capas QGIS no se versionan
gama/includes/casas_carolina.*
gama/includes/calles_carolina.*

# ── GAMA Platform ─────────────────────────────────────────────────────────
# Archivos internos del workspace de Eclipse/GAMA
gama/.settings/
gama/snapshots/
gama/**/*.bak
gama/**/*.log
*.gaml.bak

.metadata/
*.bak_gaml
.built_in_models_*

# ── Outputs de simulación ─────────────────────────────────────────────────
# Incluir los CSV de ejemplo (run1) pero ignorar ejecuciones adicionales
gama/outputs/*_run[2-9]_*.csv
gama/outputs/*_run[1-9][0-9]_*.csv

# ── Sistema operativo ─────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
*.tmp
*.swp
*~

# ── IDEs ──────────────────────────────────────────────────────────────────
.idea/
.vscode/
*.code-workspace
CLAUDE.md

# ── LaTeX / Overleaf ──────────────────────────────────────────────────────
*.aux
*.log
*.out
*.toc
*.bbl
*.blg
*.fls
*.fdb_latexmk
*.synctex.gz
